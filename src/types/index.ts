// TypeScript type definitions for HomeLink
export interface Community {
  id: string;
  name: string;
  description: string;
}

export interface User {
  id: string;
  name: string;
  avatarUrl?: string;
}
